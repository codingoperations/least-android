<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RateMe" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#FBE201</item>
        <item name="colorPrimaryVariant">#FFFF00</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@android:color/darker_gray</item>
        <item name="background">@android:color/darker_gray</item>

        <item name="colorControlNormal">?attr/colorPrimaryVariant</item>
        <item name="colorControlActivated">?attr/colorPrimary</item>

    </style>
</resources>